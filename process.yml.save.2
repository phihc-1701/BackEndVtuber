require('dotenv').config();
  apps : [{
    name: "prod_deployjanus",
    script: "./app.js",
    watch: true,
    instances: "1",
    exec_mode: cluster,
    env_production: {
      NODE_ENV: process.env.NODE_ENV	
    }
]
 
